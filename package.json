{
  "name": "slate-teaching-board",
  "version": "1.0.0",
  "description": "Private teaching digital board with Cloudflare Workers serverless deployment.",
  "main": "worker.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
